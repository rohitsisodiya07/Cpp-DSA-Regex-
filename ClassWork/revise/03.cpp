#include <bits/stdc++.h>
using namespace std;

int main()
{

    // vector<int> v = {5, 15, 25, 35, 10, 3};
    // int pos = 0, neg = 0 ;
    // int maxi = INT_MIN;
    // int mini = INT_MAX ;
    // int sum = 0 ;
    // int count = 0 ;
    // for (auto ch : v)
    // {

    //     sum += ch ;
    //     count++;
    //     maxi = max(maxi, ch);
    //     mini = min(mini, ch) ;
    //     if( ch % 2 == 0 ) pos++ ;
    //     else neg++ ;
    // }
    // cout << " Largest Element = " << maxi <<endl;
    // cout << " Smallest Element = " << mini <<endl ;
    // cout << " Sum of Array = " << sum << endl ;
    // cout << "Average of Array = " << sum/count <<endl ;
    // cout << "Positive Elements = " << pos <<endl;
    // cout << "Negative Elements = " << neg <<endl;

    //************************************************************ */
    // Medium Level
    // vector<int> v = {12, 14, 25, 63, 42, 22};
    // int largest = INT_MIN;
    // int second = INT_MIN;
    // for (auto ch : v)
    // {

    //     if (ch > largest)
    //     {
    //         second = largest;
    //         largest = ch;
    //     }
    //     else if (ch < largest && ch > second)
    //     {
    //         second = ch;
    //     }
    // }
    // cout << "Second Largest Element = " << second;

    // vector<int> v = {12, 14, 25, 63, 42, 22};
    // int smallest = INT_MAX;
    // int second = INT_MAX;
    // for (auto ch : v)
    // {

    //     if ( smallest > ch )
    //     {
    //         second = smallest;
    //         smallest = ch;
    //     }
    //     else if (ch > smallest && ch < second)
    //     {
    //         second = ch;
    //     }
    // }
    // cout << "Second Smallest Element = " << second;

    // vector <int> v = {10, 15, 20, 30, 40, 10, 40, 50, 30, 40, 10, 20} ;
    // map <int, int> m ;
    // for( auto ch : v){
    //     m[ch]++ ;
    // }
    // for( auto ch : m){
    //     cout << ch.first << " -> " << ch.second <<endl;
    // }

    // vector<int> v = {10, 15, 40, 30, 20, 10, 15};
    // set<int> second;
    // vector<int> v2;
    // for (auto ch : v)
    // {

    //     if (!second.count(ch))
    //     {

    //         v2.push_back(ch);
    //         second.insert(ch);
    //     }
    // }
    // for (auto ch : v2)
    // {
    //     cout << ch << endl;
    // }

    // bubble-sort(ascendong).
    // vector<int> v = {10, 5, 3, 40, 2, 6, 8};
    // int n = v.size() - 1;
    // for (int i = 0; i < n; i++)
    // {

    //     for (int j = 0; j < n - i; j++)
    //     {

    //         if (v[j] > v[j + 1])
    //         {
    //             swap(v[j], v[j + 1]);
    //         }
    //     }
    // }
    // for( auto ch : v){
    //     cout << ch << " " ;
    // }


    //selection sort(ascending) ;
    


}