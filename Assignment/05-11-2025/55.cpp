#include<bits/stdc++.h>
using namespace std;

int main()
{
	int a, b, c, d, e, f, g, h, i, j, sum;
	cout << "Enter Value = ";
	cin >> a >> b >> c >> d >> e >> f >> g >> h >> i >> j;
	sum = a + b + c + d + e + f + g + h + i + j;
	cout << "Sum = " <<sum;
}