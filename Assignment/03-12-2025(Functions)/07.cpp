#include<bits/stdc++.h>
using namespace std;

void  displayRules(){
    cout << "Rules of Cricket:" << endl ;
    cout << "Each team gets to bat only once and tries to score maximum runs." <<endl ;
    cout << "A batsman is out if the ball hits the stumps, is caught, or is run out."<<endl;
    cout <<"The team with the highest total runs at the end of the match wins.";
}

int main()
{
	 displayRules();
}